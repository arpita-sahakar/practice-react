import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
//render the app container in "root" div. The "root" div is located in index.html
ReactDOM.render(<App />, document.getElementById("root"));
